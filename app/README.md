# App (routes)

File-based routing with **expo-router**. See the [project README](../README.md) for the full structure.

- `(auth)` — Login and signup.
- `(main-screens)` — Tab navigator: Home, Calendar, Progress, Explorer, Settings.
